package com.harshalshah.floorplanner.ui;

/** Layout constants shared across canvas components. */
final class UiConstants {

    /** Width of the right-hand control panel — the canvas's usable area stops here. */
    static final int CONTROL_PANEL_WIDTH = 320;

    private UiConstants() {
    }
}
